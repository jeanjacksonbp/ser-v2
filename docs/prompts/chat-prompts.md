# Prompts — Chat (contexto fixo)

[CONTEXTO FIXO — SER v2]
- Stack: PHP 8.3 + MySQL, MVC fino, RBAC+ABAC (Guard::can + ActorContext), middleware de escopo.
- Padrões: PSR-12, prepared statements, nada de regra nas views.
- Objetivo desta conversa: <especificar a feature>

Pedido-tipo:
“Gerar spec + arquivos base para [FEATURE]; incluir SQL idempotente; ‘Como validar’ (curl/rotas) e mensagens de commit/PR.”
